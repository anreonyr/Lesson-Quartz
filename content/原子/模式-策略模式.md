---
id: 202603090035
aliases:
  - 策略
  - Strategy
tags:
  - pattern
date: 2026-03-09
passed: false
---

# 策略模式

**意图**：定义一系列算法，把它们一个个封装起来，使它们可以相互替换。

## 解决的问题

- **接口简化**：调用方只需调用 `sort()`，不需要知道用了什么算法
- **解耦**：切换算法时不需要修改使用排序的代码

## 适用场景

- 不同的排序算法
- 不同的支付策略
- 不同的压缩算法

## 生活例子

就像出行方式，你可以开车、坐地铁、骑自行车，都可以到达目的地。

## 代码示例

```python
from abc import ABC, abstractmethod

class SortStrategy(ABC):
    @abstractmethod
    def sort(self, data):
        pass

class QuickSort(SortStrategy):
    def sort(self, data):
        return sorted(data)  # 简化示例

class BubbleSort(SortStrategy):
    def sort(self, data):
        result = data.copy()
        n = len(result)
        for i in range(n):
            for j in range(0, n-i-1):
                if result[j] > result[j+1]:
                    result[j], result[j+1] = result[j+1], result[j]
        return result

class Sorter:
    def __init__(self, strategy: SortStrategy):
        self.strategy = strategy

    def set_strategy(self, strategy):
        self.strategy = strategy

    def sort(self, data):
        return self.strategy.sort(data)

# 使用 - 切换算法不需要修改调用方
sorter = Sorter(QuickSort())
print(sorter.sort([3, 1, 4, 1, 5]))  # [1, 1, 3, 4, 5]

sorter.set_strategy(BubbleSort())
print(sorter.sort([3, 1, 4, 1, 5]))  # [1, 1, 3, 4, 5]
```

## AI编程中的应用

> 请用策略模式实现一个排序功能，支持快速排序、冒泡排序、归并排序，用户可以切换不同的排序策略。
