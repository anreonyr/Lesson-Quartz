---
id: 202603090034
aliases:
  - 观察者
  - Observer
tags:
  - pattern
date: 2026-03-09
passed: false
---

# 观察者模式

**意图**：定义对象之间的一对多依赖关系，当一个对象改变时，所有依赖它的对象都会收到通知。

## 解决的问题

- **接口简化**：发布者只需调用 `notify()`，不需要知道有多少订阅者
- **解耦**：发布者和订阅者之间没有直接依赖，可以独立变化

## 适用场景

- 事件系统
- 消息推送
- 股票价格更新

## 生活例子

就像微信公众号关注，你关注后，作者发文章你会收到通知。

## 代码示例

```python
class Subject:
    def __init__(self):
        self._observers = []

    def attach(self, observer):
        self._observers.append(observer)

    def detach(self, observer):
        self._observers.remove(observer)

    def notify(self, message):
        for observer in self._observers:
            observer.update(message)

class Observer:
    def __init__(self, name):
        self.name = name

    def update(self, message):
        print(f"{self.name} 收到通知: {message}")

# 使用 - 发布者和订阅者解耦，互不关心
stock = Subject()
user1 = Observer("张三")
user2 = Observer("李四")
stock.attach(user1)
stock.attach(user2)
stock.notify("股票涨了5%!")
# 输出:
# 张三 收到通知: 股票涨了5%!
# 李四 收到通知: 股票涨了5%!
```

## AI编程中的应用

> 请用观察者模式实现一个简单的新闻订阅系统，当有新闻发布时，所有订阅者都会收到通知。
